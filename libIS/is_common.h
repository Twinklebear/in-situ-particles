
#define QUERY_BOXES
// WILL: We support 3 floats for position (x, y, z) and one
// additional float for the attribute
#define OSP_IS_STRIDE_IN_FLOATS 4
// Enable if we want to show an accurate count of all particles on the
// simulation being rendered. The renderer actually renders more than this
// due to the ghost zones.
#define OSP_IS_PRINT_TOTAL_PARTICLES

